import 'package:flutter/material.dart';

import 'package:disenios/src/widgets/headers.dart';

class HeadersPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: HeaderGradiente()
    );
  }
}